version: "3.8"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - AQI_KEY=${AQI_KEY} # Passes the key from your local .env file
    volumes:
      - .:/app
